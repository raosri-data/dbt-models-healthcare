version: 2

sources:
  - name: raw
    tables:
      - name: claims

models:
  - name: stg_claims
    description: "Staging table for claims data"
    columns:
      - name: claim_id
        tests:
          - not_null
          - unique

  - name: mart_provider
    description: "Aggregated provider metrics"
